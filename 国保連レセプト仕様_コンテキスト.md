# 障害福祉サービス 国保連レセプト（CSV）出力仕様書ガイドライン

本プロジェクトでは、厚生労働省・国保中央会が定める「障害者総合支援給付審査支払等システム インタフェース仕様書」に準拠した伝送用CSVファイルを生成します。

---

## 1. ファイル全体構造 (File Structure)

国保連伝送用CSVは、必ず以下の3種類のレコード構成（3層構造）で生成しなければなりません。

1. **コントロールレコード（先頭1行目 / Record Type 1）**
   - ファイルのヘッダー情報（送信元、対象年月、データ種別等）
2. **データレコード（2行目〜最終行の前行 / Record Type 2）**
   - 請求書、明細書、実績記録票などの実データ
3. **エンドレコード（最終行 / Record Type 3）**
   - ファイルの終端情報（全行数のカウント）

---

## 2. ファイルフォーマット規定 (Format Specifications)

- **文字コード**: Shift-JIS (CP932 / Windows-31J)
- **改行コード**: CRLF (`\r\n` / 16進数 `0x0D0A`)
- **セパレータ**: カンマ (`,`)
- **ファイル拡張子**: `.CSV` (英数半角8桁以内 + `.CSV`)
- **ダブルクォーテーション**: 
  - カンマ、スペース、漢字を含む文字列項目は `"` で囲む。
  - 数字・コード値等で区切り文字を含まない場合は省略可。

---

## 3. レコード仕様詳細

### (1) コントロールレコード (Record Type 1)

| 項番 | 項目名 | 属性 | バイト数 | 設定内容・条件 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | レコード種別 | コード値 | 1 | 固定値 `1` |
| 2 | レコード番号 | 数値 | 9 | 通番（先頭行のため `1`） |
| 3 | ボリューム通番 | 数値 | 3 | 固定値 `0`（伝送時） |
| 4 | レコード件数 | 数値 | 9 | **データレコード（種別2）の総件数**（コントロール・エンドは除外） |
| 5 | データ種別 | 英数 | 3 | 代表データ種別（例: `J11` 介護給付費等請求, `K11` 障害児等） |
| 6 | 市町村番号 | コード値 | 6 | 事業所からの送信時は `0` または `000000` |
| 7 | 事業所番号 | 英数 | 10 | 10桁の指定事業所番号 |
| 8 | 都道府県番号 | コード値 | 2 | 事業所からの送信時は `0` または `00` |
| 9 | 媒体区分 | 英数 | 1 | 固定値 `1`（インターネット伝送） |
| 10 | 処理対象年月 | コード値 | 6 | 審査実行年月 (YYYYMM例: `202603`) |
| 11 | 予備 | 数値 | 6 | 設定なし（ブランク） |

### (2) データレコード (Record Type 2)

| 項番 | 項目名 | 属性 | バイト数 | 設定内容・条件 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | レコード種別 | コード値 | 1 | 固定値 `2` |
| 2 | レコード番号 | 数値 | 9 | ファイル内の連番 (2から始まる通番) |
| 3 | データ | 可変 | 可変 | 交換情報に応じたCSVデータ項目群 |

#### 代表的なサービス種類コード (サービス種別)
- `11`: 居宅介護 / `12`: 重度訪問介護
- `22`: 生活介護 / `24`: 短期入所
- `33`: 共同生活援助 (グループホーム)
- `43`: 就労移行支援 / `45`: 就労継続支援A型 / `46`: 就労継続支援B型
- `52`: 計画相談支援

#### 代表的な様式・データ種別 (申立事由・請求データ)
- `J11` / `461`: 介護給付費・訓練等給付費等請求書
- `J21` / `462`: 介護給付費・訓練等給付費等明細書
- `J61` / `473`: サービス提供実績記録票

### (3) エンドレコード (Record Type 3)

| 項番 | 項目名 | 属性 | バイト数 | 設定内容・条件 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | レコード種別 | コード値 | 1 | 固定値 `3` |
| 2 | レコード番号 | 数値 | 9 | ファイル内の最終行番号（例: 全5行なら `5`） |

---

## 4. バリデーションルール & 開発上の注意点

1. **ゼロ埋め（前ゼロ）と桁数厳守**:
   - 事業所番号（10桁）、受給者証番号（10桁）など、桁数が足りない場合は左側を `0` で埋める（例: `0000012345`）。
2. **日付フォーマット**:
   - 年月日: 西暦8桁（YYYYMMDD 例: `20260201`）
   - 年月: 西暦6桁（YYYYMM 例: `202602`）
3. **未設定項目の扱い**:
   - 任意項目でデータがない場合は値を空（カンマのみ `,,`）として出力する。
